---
title: 顺通测试服务器维护清单
type: reference
tags:
  - mall4cloud
  - 测试环境
  - 服务器信息
  - 部署信息
  - 敏感
created: 2026-09-10
updated: 2026-09-10
resource: "124.128.84.22"
---

# 顺通测试服务器维护清单

> [!warning] 内部敏感资料
> 本文包含测试环境内网 IP、服务拓扑与部署路径，仅限项目成员使用，请勿提交到公开仓库或对外发送。账号密码不在此文档记录，凭据由 Droid SSH 连接配置与服务器 `.env` 管理。

> **更新时间**：2026-09-10
> **接入入口**：`124.128.84.22`（Droid SSH MCP 配置端口 `60123` / `60124`）
> **HTTPS 证书**：通配符 `*.sdstsh.cn`（DigiCert RapidSSL）

## 目录

1. [[#一、服务器清单]]
2. [[#二、后端服务拆分]]
3. [[#三、前端访问地址]]
4. [[#四、中间件]]
5. [[#五、Nginx 部署]]
6. [[#六、项目地址（本地仓库 ↔ 部署目录）]]
7. [[#七、变更记录]]

## 一、服务器清单

| 名称 | 角色 | 内网 IP | 主机名 | SSH 入口（Droid MCP） |
| --- | --- | --- | --- | --- |
| 商城测试1 | 前端、网关、部分后端、全部中间件 | `192.168.1.252` | `mall4test-252` | `root@124.128.84.22:60123` |
| 商城测试2 | 部分后端 | `192.168.1.253` | `didaima` | `root@124.128.84.22:60124` |

- 两服务器处于同一内网网段（`192.168.1.0/24`），通过外网入口 `124.128.84.22` 的不同 SSH 端口接入。
- 商城测试1 还带 Docker 网桥地址 `172.17.0.1 / 172.18.0.1 / 172.19.0.1`；商城测试2 另有一个 `192.168.6.249` 网卡地址。

## 二、后端服务拆分

后端共 16 个服务，按“网关/核心 + 周边”拆分部署在两个服务器上，全部为 Docker 容器、主机网络模式直连端口。

### 商城测试1（6 个服务）

| 服务 | 端口 |
| --- | ---: |
| Gateway | `8000` |
| Auth | `9101` |
| Admin | `9112` |
| Order | `9106` |
| Payment | `9113` |
| Biz | `9118` |

### 商城测试2（10 个服务）

| 服务 | 端口 |
| --- | ---: |
| Product | `9104` |
| User | `9105` |
| Search | `9108` |
| Marketing | `9109` |
| Group | `9114` |
| Seckill | `9115` |
| Flow | `9116` |
| Workflow | `9117` |
| Invoice | `9119` |
| Alliance | `9120` |

> 前端业务请求统一走网关 `mall4cloud_*` 前缀（Nginx 代理到 `127.0.0.1:8000`），不直连业务服务端口；上表端口仅用于联调与排障。

## 三、前端访问地址

前端静态资源全部部署在商城测试1 的 Nginx 容器，统一 HTTPS（通配符证书 `*.sdstsh.cn`）。

| 端 | 访问域名 | 路径 | 部署目录（容器） |
| --- | --- | --- | --- |
| 平台管理端 | `platformtest.sdstsh.cn` | `/` | `/usr/share/nginx/html/platform` |
| 商家管理端 | `multishoptest.sdstsh.cn` | `/` | `/usr/share/nginx/html/multishop` |
| 供应商管理端 | `suppliertest.sdstsh.cn` | `/` | `/usr/share/nginx/html/supplier` |
| PC 商城 | `pctest.sdstsh.cn` | `/` | `/usr/share/nginx/html/pc` |
| PC 商城（别名） | `shop-pctest.sdstsh.cn` | `/` | `/usr/share/nginx/html/pc` |
| 用户 H5 | `h5test.sdstsh.cn` | `/h5/` | `/usr/share/nginx/html/h5` |
| 用户 H5（别名） | `shop-h5test.sdstsh.cn` | `/h5/` | `/usr/share/nginx/html/h5` |
| 门店 H5 | `h5test.sdstsh.cn`（及 `shop-h5test`） | `/station/` | `/usr/share/nginx/html/station` |
| 商家移动端 H5 | `multishop-h5test.sdstsh.cn` | `/multishop-h5/` | `/usr/share/nginx/html/multishop-h5` |
| API Gateway | `apitest.sdstsh.cn` | `/mall4cloud_*` | 代理 `127.0.0.1:8000` |
| IM WebSocket | `imtest.sdstsh.cn` | `/` | 代理 `$im_backend`（WuKongIM） |
| MinIO 对象存储 | `miniotest.sdstsh.cn` | `/`、`/minio` | 代理 `127.0.0.1:9000` / `9001` |

> 注意：三个 uni-app 项目打包时资源路径写死为绝对前缀，nginx 必须按前缀挂载，否则资源会 404 并回退到 `index.html`（表现为 JS 的 `text/html` MIME 报错）：
> - 用户 H5：`/h5/`
> - 商家移动端 H5：`/multishop-h5/`
> - 门店 H5：`/station/`
>
> 根路径 `/` 已配置 301 跳转：`h5test`、`shop-h5test` 跳到 `/h5/`，`multishop-h5test` 跳到 `/multishop-h5/`。

## 四、中间件

中间件全部部署在商城测试1（`192.168.1.252`）。

| 中间件 | 地址 | 端口 |
| --- | --- | ---: |
| MySQL | `192.168.1.252` | `3306` |
| Redis | `192.168.1.252` | `6379` |
| MongoDB | `192.168.1.252` | `27017` |
| Elasticsearch | `192.168.1.252` | `9200` |
| Nacos | `192.168.1.252` | `8848` |
| RocketMQ NameServer | `192.168.1.252` | `9876` |
| RocketMQ Broker | `192.168.1.252` | `10911` |
| RocketMQ Dashboard | `192.168.1.252` | `8180` |
| XXL-Job | `192.168.1.252` | `8090` |
| Seata | `192.168.1.252` | `8091` |
| MinIO API | `192.168.1.252` | `9000` |
| MinIO Console | `192.168.1.252` | `9001` |
| WuKongIM HTTP | `192.168.1.252` | `5001` |
| WuKongIM WebSocket | `192.168.1.252` | `5200` |
| Canal | 商城测试1 内部 | MySQL binlog → RocketMQ `canal-topic` |

> 中间件账号密码不在本文记录，实际值以商城测试1 服务器 `.env` 为准。

## 五、Nginx 部署

- 容器：`mall4cloud-nginx`，主机网络模式，监听 `80` / `443`。
- 挂载映射（宿主机 ↔ 容器）：
  - `/data/nginx/html` ↔ `/usr/share/nginx/html`（前端静态文件）
  - `/data/nginx/conf.d` ↔ `/etc/nginx/conf.d`（站点配置）
  - `/data/nginx/ssl` ↔ `/ssl`（证书）
- 证书文件：`/data/nginx/ssl/sdstsh.cn.pem` + `/data/nginx/ssl/sdstsh.cn.key`（通配符 `*.sdstsh.cn`）。

`/data/nginx/conf.d` 站点配置：

| 文件 | 作用 |
| --- | --- |
| `00-http-redirect.conf` | 80 端口默认 301 跳 HTTPS，ACME 校验路径放行 |
| `cloud-api.conf` | `apitest` → 网关 |
| `cloud-h5.conf` | `h5test` → `/h5` + `/station` |
| `cloud-shop-h5.conf` | `shop-h5test` → `/h5` + `/station` |
| `cloud-pc.conf` | `pctest` |
| `cloud-shop-pc.conf` | `shop-pctest` |
| `cloud-platform.conf` | `platformtest` |
| `cloud-multishop.conf` | `multishoptest` |
| `cloud-ms.conf` | `multishop-h5test` → `/multishop-h5` |
| `cloud-supplier.conf` | `suppliertest` |
| `cloud-minio.conf` | `miniotest` |
| `cloud-im.conf` | `imtest` |
| `status.conf` | `/stub_status`（已限制内网 ACL） |

## 六、项目地址（本地仓库 ↔ 部署目录）

工作区根目录：`D:/Work/stsh-project/mall4cloud`

| 本地仓库 | 类型 | 用途 | 部署目录（测试1） |
| --- | --- | --- | --- |
| `mall4cloud` | Java 后端 | 商城后端微服务（16 服务） | 按服务拆分到测试1/测试2 |
| `mall4cloud-generator` | Java 工具 | 代码生成器 | 不部署 |
| `mall4cloud-doc` | 上游只读文档 | 文档查阅 | 不部署 |
| `mall4cloud-platform` | Vue 3 + Vite | 平台管理端 | `platform` |
| `mall4cloud-multishop` | Vue 3 + Vite | 商家管理端 | `multishop` |
| `mall4cloud-pc` | Vue 3 + Vite | PC 商城 | `pc` |
| `mall4cloud-slipper` | Vue 3 + Vite | 供应商管理端（仓库名与部署目录 `supplier` 不一致） | `supplier` |
| `mall4cloud-uniapp` | uni-app | 用户端 H5/小程序/App | `h5`（base `/h5/`） |
| `mall4cloud-multishop-uniapp` | uni-app | 商家移动端 H5/小程序/App | `multishop-h5`（base `/multishop-h5/`） |
| `mall4cloud-station` | uni-app | 门店 H5 | `station`（base `/station/`） |

## 七、变更记录

### 2026-09-10 Nginx 审计与修复

- 修复历史遗留 `cloud-*.conf` 的容器内无效 `root /data/nginx/html/...` 路径，统一改为 `/usr/share/nginx/html/...`。
- API / IM / MinIO 上游修正为本地可达地址（网关 `127.0.0.1:8000`，MinIO `127.0.0.1:9000/9001`，IM `$im_backend`）。
- `server_name` 统一改为测试域名（`*test.sdstsh.cn`）。
- `status.conf` 的 `/stub_status` 增加内网 ACL（`allow 127.0.0.1`、`192.168.1.0/24`、`172.16.0.0/12`、`10.0.0.0/8`，`deny all`）。
- 新增 `00-http-redirect.conf`，80 端口默认 301 跳 HTTPS。
- 备份：`/data/nginx/conf.d.bak-20260910_171932`。

### 2026-09-10 证书替换

- 原证书仅覆盖主域名 `sdstsh.cn / www.sdstsh.cn`，子域名访问报 `ERR_CERT_COMMON_NAME_INVALID`。
- 已替换为通配符证书 `*.sdstsh.cn`（DigiCert RapidSSL），reload 后经 `openssl s_client` 验证 SAN 含 `*.sdstsh.cn`。

### 2026-09-10 H5 基础路径 MIME 修复

- 根因：uni-app 打包资源使用绝对前缀，而站点曾把 `root` 指到子目录，导致 `/h5/assets/...` 实际解析到 `/h5/h5/assets/...`，404 后回退 `index.html`（`text/html`）。
- 修复：`cloud-shop-h5.conf`、`cloud-ms.conf` 改为前缀挂载（`/h5`、`/multishop-h5`）+ 根路径 301；`cloud-h5.conf` 同步为 `/h5` 挂载。
- 验证：`h5test`、`shop-h5test`、`multishop-h5test` 的 JS 资源均返回 `application/javascript`，`nginx -t` 通过。
- 备份：`cloud-shop-h5.conf.bak-20260910_175833`、`cloud-ms.conf.bak-20260910_175833`。
